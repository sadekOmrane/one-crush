import 'package:flutter/cupertino.dart';

class AccountAppBarWidget extends StatelessWidget {
  const AccountAppBarWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
